# Optimized requirements for t3.micro
# Using CPU-only PyTorch to reduce memory usage

Flask==3.1.2
gunicorn==23.0.0
Pillow==12.0.0

# CPU-only PyTorch (much smaller)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# Use compatible numpy version with PyTorch
numpy==2.1.3

# Scikit-learn
scikit-learn==1.5.2

# Streamlit and dependencies
streamlit>=1.29.0
pymongo==4.10.1
requests==2.32.3
